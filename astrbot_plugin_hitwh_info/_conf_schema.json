{
  "postgres_dsn": {
    "description": "PostgreSQL DSN，例如 postgresql://user:pass@localhost:5432/dbname。数据库需可创建 pgvector 和 pg_trgm 扩展。",
    "type": "string",
    "default": ""
  },
  "token": {
    "description": "教务系统 Cookie。推荐通过 http://localhost:8888 一键捕获后自动保存。",
    "type": "text",
    "default": "",
    "hint": "手动填写时复制成绩页 /cjcx/queryQmcj 请求头里的 Cookie 值，不要带 Cookie: 前缀。",
    "obvious_hint": true
  },
  "webvpn_base": {
    "description": "IVPN/WebVPN 教务基础地址。教务接口路径已在代码中固定。",
    "type": "string",
    "default": "http://jwts-hitwh-edu-cn.ivpn.hitwh.edu.cn:8118",
    "hint": "通常保持默认；如使用 webvpn.hitwh.edu.cn/http/... 代理地址，可填其不带业务路径的基础 URL。"
  },
  "colleges": {
    "description": "可选院系列表；为空时使用内置 HITWH 常见学院并从网页正文补充。",
    "type": "list",
    "items": {
      "type": "string"
    },
    "default": []
  },
  "sync_interval_hours": {
    "description": "自动同步间隔（小时）",
    "type": "int",
    "default": 1,
    "hint": "每 N 小时自动同步成绩、课表、考试和培养方案；当前小于等于 0 会按 1 小时处理。"
  },
  "group_whitelist": {
    "description": "QQ群消息采集白名单，群号列表。为空时不采集任何群消息。",
    "type": "list",
    "items": { "type": "string" },
    "default": []
  },
  "embedding_api_base": {
    "description": "嵌入模型 API 地址",
    "type": "string",
    "default": "https://api.siliconflow.cn/v1",
    "hint": "推荐使用硅基流动: https://api.siliconflow.cn/v1"
  },
  "embedding_api_key": {
    "description": "嵌入模型 API Key",
    "type": "string",
    "default": "",
    "hint": "在 https://siliconflow.cn 注册获取；用于 /索引 和语义搜索。",
    "obvious_hint": true
  },
  "embedding_model": {
    "description": "嵌入模型名称",
    "type": "string",
    "default": "BAAI/bge-m3",
    "options": ["BAAI/bge-m3", "BAAI/bge-large-zh-v1.5", "Pro/BAAI/bge-m3"]
  },
  "embedding_dim": {
    "description": "嵌入向量维度，需与模型匹配",
    "type": "int",
    "default": 1024,
    "hint": "bge-large-zh-v1.5=1024, bge-m3=1024"
  },
  "rerank_api_base": {
    "description": "重排模型 API 地址",
    "type": "string",
    "default": "https://api.siliconflow.cn/v1"
  },
  "rerank_api_key": {
    "description": "重排模型 API Key",
    "type": "string",
    "default": "",
    "hint": "可与嵌入模型共用同一个 Key。",
    "obvious_hint": true
  },
  "rerank_model": {
    "description": "重排模型名称",
    "type": "string",
    "default": "BAAI/bge-reranker-v2-m3",
    "options": ["BAAI/bge-reranker-v2-m3"]
  }
}
